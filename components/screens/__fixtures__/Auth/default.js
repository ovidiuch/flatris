// @flow

import Auth from '../../Auth';

const fixture = {
  component: Auth,

  props: {},
  reduxState: {},

  container: {
    width: 10,
    fullHeight: true,
    backgroundColor: 'rgba(236, 240, 241, 0.85)'
  }
};

export default fixture;
