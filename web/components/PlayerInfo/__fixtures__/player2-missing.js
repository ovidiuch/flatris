// @flow

import React from 'react';
import PlayerInfo from '..';

export default (
  <PlayerInfo
    player={null}
    wins={null}
    isPlayer1={false}
    showReadyState={false}
    onSelect={() => console.log('Select P2')}
  />
);
